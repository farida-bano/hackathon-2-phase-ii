{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "gunicorn -k uvicorn.workers.UvicornWorker -b 0.0.0.0:$PORT src.main:app --workers 4",
    "healthcheckPath": "/",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}
